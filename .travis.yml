language: rust
before_install:
    - rustup component add rustfmt
script:
    - cargo fmt -- --check
    - cargo build --verbose
    - cargo test --verbose
rust:
    - stable
    - beta
    - nightly
jobs:
    allow_failures:
        - rust: nightly
    fast_finish: true
cache: cargo
